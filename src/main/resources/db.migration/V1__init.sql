CREATE TABLE afods
(
    id                  uuid,
    cod_ent             VARCHAR,
    id_list_negra       VARCHAR,
    cod_list_negra      VARCHAR,
    fch_timestamp_umo   TIMESTAMP,

    PRIMARY KEY (id)
);
